
					<!--This code generated from tDev Powered by Mahat, Build Number :#2024-01-001(Updated on 06-01-2024 12:57PM) on 08/01/2026 08:07:28 -->
					@using Microsoft.AspNetCore.Http
					@inject IHttpContextAccessor HttpContextAccessor
					@using Microsoft.Extensions.Configuration
					@inject IConfiguration Configuration
					@using Admin;
					@model NalamVazha.Models.ArrivalModel
					@{
					ViewData["Title"] = @Resource.arrivallist;
					Layout = "~/Views/Shared/_Layout.cshtml";
					}
					<link href="~/css/report-kpi.css" rel="stylesheet" asp-append-version="true" />

					<div class="card " style="padding:20px;">
					<div class="mobileHeader"><!--form_card_header-->
					<div class="row">
					<div class="col-sm-12 col-xs-12">
					<h3 class="formHdr">@Resource.arrivallist</h3>
					</div>
					</div>
					</div><!--form_card_header-->
					<div class="card-body" style="padding:1px;padding-top:0px;">

					@if(TempData["errMessage"] != null)
					{
					<div class="row">
					<div class="col-sm-12 col-xs-12">
					<div class="alert alert-danger alert-dismissible ">
					<button type="button" class="close" data-dismiss="alert" aria-label="Close">
					<span aria-hidden="true">x</span>
					</button>
					Info :  @TempData["errMessage"]
					</div>
					</div>
					</div>
					}

					@if(TempData["message"] != null)
					{
					<div class="row">
					<div class="col-sm-12 col-xs-12">
					<div class="alert alert-success alert-dismissible ">
					<button type="button" class="close" data-dismiss="alert" aria-label="Close">
					<span aria-hidden="true">x</span>
					</button>
					Info :  @TempData["message"]
					</div>
					</div>
					</div>
					}

					<input type="hidden" id="Arrivalid" name="Arrivalid"/>
					<div class="row">
								<div class="col-sm-2 col-xs-12 p-2">
								<div>
								<div class="form-group form-group--float-above my-3" > 
								<label for="tenantid" class="tenant_display">@Resource.tenantdisplayname</label>
								<select name="tenantid" id="tenantid" class="form-control">
								</select>
								<i class="form-group__bar"></i>
								</div>

								<div id="tenantid-error" class="help-block" style="display: none" role="alert"></div>
								</div>
								</div>
								<div class="col-sm-2 col-xs-12 p-2 ipdnumber_craftmyapp_container">
								<div>
									<div class="form-group form-group--float-above my-3" > 
										<label for="ipdnumber">@Resource.arrival_ipdnumber</label>
										<select name="ipdnumber" id="ipdnumber" class="form-control  "></select>

									</div>
									@Html.ValidationMessageFor(model => model.ipdnumber)
									<div id="ipdnumber-error" class="help-block" style="display: none" role="alert"></div>
								</div>
							</div>
<div class="col-sm-2 col-xs-12 p-2 patient_craftmyapp_container">
								<div>
									<div class="form-group form-group--float-above my-3" > 
										<label for="patient">@Resource.arrival_patient</label>
										<select name="patient" id="patient" class="form-control  "></select>

									</div>
									@Html.ValidationMessageFor(model => model.patient)
									<div id="patient-error" class="help-block" style="display: none" role="alert"></div>
								</div>
							</div>
<div class="col-sm-2 col-xs-12 p-2 room_craftmyapp_container">
								<div>
									<div class="form-group form-group--float-above my-3" > 
										<label for="room">@Resource.arrival_room</label>
										<select name="room" id="room" class="form-control  "></select>

									</div>
									@Html.ValidationMessageFor(model => model.room)
									<div id="room-error" class="help-block" style="display: none" role="alert"></div>
								</div>
							</div>
<div class="col-sm-2 col-xs-12 p-2 estimatedarrival_craftmyapp_container">
								<div>
									<div class="form-group form-group--float-above my-3" > 
										<label for="estimatedarrival">@Resource.arrival_estimatedarrival</label>
										<input type="text" name="estimatedarrival" id="estimatedarrival" class="form-control" autocomplete="off" placeholder="dd/mm/yyyy" value="@DateTime.Today.ToString("dd/MM/yyyy")" />

									</div>
									@Html.ValidationMessageFor(model => model.estimatedarrival)
									<div id="estimatedarrival-error" class="help-block" style="display: none" role="alert"></div>
								</div>
							</div>
<div class="col-sm-2 p-2">

												<button id="btnFilter" class="btn btn-secondary btn--icon-text mt-5"><i class="fa fa-filter"></i> Filter</button>

												</div>
								</div>

					<div class="report-kpi-grid" aria-label="Arrival summary">
						<div class="report-kpi"><div class="report-kpi__label">Total Arrivals</div><div class="report-kpi__value" id="arrivalKpiTotal">0</div></div>
						<div class="report-kpi report-kpi--blue"><div class="report-kpi__label">Arrival Confirmed</div><div class="report-kpi__value" id="arrivalKpiConfirmed">0</div></div>
						<div class="report-kpi report-kpi--amber"><div class="report-kpi__label">Assistance Required</div><div class="report-kpi__value" id="arrivalKpiAssistance">0</div></div>
					</div>

					<div class="row"><div class="col-sm-12 col-xs-12">
					<div class="table-responsive" style="margin:0px;">
					<table id="tblArrival" class="table order-column hover responsive nowrap mahatTableWidthSetter" style="margin:0px; ">
					<thead class="thead-default">
					<tr>
					
					<th>@Resource.arrival_ipdnumber</th>
<th>@Resource.arrival_patient</th>
<th>@Resource.arrival_room</th>
<th>@Resource.arrival_estimatedarrival</th>
<th>@Resource.arrival_bookingstatus</th>
<th>@Resource.arrival_travelarrangement</th>
<th>@Resource.arrival_pickupfrom</th>
<th>@Resource.arrival_wheelchairassistance</th>
<th>@Resource.arrival_requireddinner</th>
<th>@Resource.arrival_specialrequest</th>
<th>@Resource.arrival_paymentstatus</th>
<th>@Resource.arrival_pendingamount</th>
<th>@Resource.tenantdisplayname</th>

					<th class="never">Action</th>
					

					</tr>
					</thead>
					<tbody></tbody>
					
					</table>
					</div>

					</div>
					
					
					</div>
					
					</div>
					</div>
					
					<link href="~/components/jquery-colorbox/colorbox.css" rel="stylesheet" />
					<script src="~/components/jquery-colorbox/jquery.colorbox.js"></script>
					<link href="~/css/GallaryContent/style.css" rel="stylesheet" />
					<link href="~/components/starrr/starrr.css" rel="stylesheet" />
					
					 <script src="~/js/dtpager.js"></script>

					<script type="text/javascript">
					function updateArrivalKpis(summary) {
						summary = summary || {};
						$('#arrivalKpiTotal').text(Number(summary.totalArrivals || 0).toLocaleString('en-IN'));
						$('#arrivalKpiConfirmed').text(Number(summary.arrivalConfirmed || 0).toLocaleString('en-IN'));
						$('#arrivalKpiAssistance').text(Number(summary.assistanceRequired || 0).toLocaleString('en-IN'));
					}

					var arr_selected_for_verification = [];

					function fill_Arrival()
					{

						var sendFilterData = {
								"tenantid":($("#tenantid").val() ? $("#tenantid").val() : str_tenantid),
								"ipdnumber":$("#ipdnumber").val(),
"patient":$("#patient").val(),
"room":$("#room").val(),
"estimatedarrival":$("#estimatedarrival").val(),

								"pagesize": pageSize,
								"pagenumber": (currentPageNumber - 1)
								}
						

					

					


					
		 dataTableId="#tblArrival";
		 reachedEOF = false;
		 totalRecordsCount = 0;
		 currentPageNumber = 1;
		 fetchingDataFromApi = false;
	
		var pagerControlsTemplate = "<div id='pager_container' class='dataTables_paginate'>"
			+ "<button type='button' id='pager_btn_previous' onClick='pager_gotoPreviousPage(this);' class='paginate_button previous'>Previous</button>"
			+ "<span>Page <input type='number' id='pager_txt_currentpage' min='1' max='^MAXPAGE^' maxlength='10' size='10' onFocus='this.oldValue=this.value;' onChange='pager_gotoPage(this);'/> of <span id='pager_lbl_pagecount'></span></span>"
			+ "<select id='pager_ddl_pagesize' onChange='pager_pageSizeChanged(this);'>"
			+ "<option value='50' selected>50 rows</option>"
			+ "<option value='100'>100 rows</option>"
			+ "<option value='200'>200 rows</option>"
			+ "<option value='500'>500 rows</option>"
			+ "<option value='^Everything^'>Everything</option>"
			+ "</select>"
			+ "<button type='button' id='pager_btn_next' onClick='pager_gotoNextPage(this);' class='paginate_button next'>Next</button>"
			+ "</div>";

	var table=$("#tblArrival").DataTable({
	columnDefs: [{ 
	 targets: -1,
	 visible: false,
	 searchable: false
  }],
	paging:false,
	destroy:true,
	serverSide: true,
	filter: true,
	"orderMulti": false,
	searchDelay: 2000,
	autoWidth: false,
	"order": [],"aaSorting": [],
	autoWidth: !1,responsive:!0,
	language: {
	searchPlaceholder: "Search for records..."
	},
	 language: { search: '<i class="fa fa-search"></i>', searchPlaceholder: "Search for records..." },
	dom: "Bfrtip", sDom: '<"dataTables__top"lfB>rt<"dataTables__bottom"ip><"clear">',
  
	buttons: [{
	extend: "excelHtml5",
	title: "List Of Arrival Data"
	}, {
	extend: "csvHtml5",
	title: "List Of Arrival Data"
	}, {
	extend: "print",
	title: "List Of Arrival Data"
	}],
 initComplete: function(a, b) {



	  $(this).closest(".dataTables_wrapper").find(".dt-buttons").hide();
	  
		if ($('#btnFilter').length && !$(this).closest(".dataTables_wrapper").find(".automatonShowFilter").length) {
	 $(this).closest(".dataTables_wrapper").find(".dataTables__top").prepend(`
		<i class="automatonShowFilter actions__item mdi material-icons">filter_alt</i>
	  `);
		 if ($("#cma_card_tab_action_container i.automatonShowFilter").length === 0) {
			$("#cma_card_tab_action_container").prepend(`
				<i class="automatonShowFilter actions__item mdi material-icons">filter_alt</i>
			`);
		}
 }

var Actions=getAutho("Arrival","create")
		if(Actions != undefined){
			var create_URL = "@Url.Action("entry.viewname", "Arrival")"
			create_URL = create_URL.replace("entry.viewname", Actions.actionmethodname);
			
if ($(this).closest(".dataTables_wrapper").find(".dataTables__top .dataTables_buttons").length == 0) {
			$(this).closest(".dataTables_wrapper").find(".dataTables__top").prepend(`   

	   
		<div class="dataTables_buttons actions">
		<div class="input-group prefix">
							<span class="input-group-addon">
							<i class="material-icons">search</i>
							</span>
							<input type="text" class="form-control dt-global-search" placeholder="Search..." />
						</div>
		  <div class="dropdown actions__item cma_top_icon">
			<i data-toggle="dropdown" class="material-icons">share</i>
			<ul class="dropdown-menu dropdown-menu-right">
			  <a href="javascript: void 0" class="dropdown-item" data-table-action="excel">Excel (.xlsx)</a>
			  <a href="javascript: void 0" class="dropdown-item" data-table-action="csv">CSV (.csv)</a>
			 
			</ul>
		  </div>
		 <div class="dropdown actions__item cma_top_icon">
									<i data-toggle="dropdown" class="material-icons">settings</i>
									<ul class="dropdown-menu dropdown-menu-right ColVis_collection">
									<li>
																<label> @Resource.arrival_ipdnumber
 <input type="checkbox" class="colvis_switch" data-column="0" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_patient
 <input type="checkbox" class="colvis_switch" data-column="1" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_room
 <input type="checkbox" class="colvis_switch" data-column="2" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_estimatedarrival
 <input type="checkbox" class="colvis_switch" data-column="3" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_bookingstatus
 <input type="checkbox" class="colvis_switch" data-column="4" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_travelarrangement
 <input type="checkbox" class="colvis_switch" data-column="5" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_pickupfrom
 <input type="checkbox" class="colvis_switch" data-column="6" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_wheelchairassistance
 <input type="checkbox" class="colvis_switch" data-column="7" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_requireddinner
 <input type="checkbox" class="colvis_switch" data-column="8" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_specialrequest
 <input type="checkbox" class="colvis_switch" data-column="9" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_paymentstatus
 <input type="checkbox" class="colvis_switch" data-column="10" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_pendingamount
 <input type="checkbox" class="colvis_switch" data-column="11" checked /></label>
															  </li>
   
									</ul>
									</div>

		 <a href="create_URL" class="btn btn-rounded add-row-btn cma_add_button">Add <span class="material-icons">add</span></a>
		</div>
	  `.replace("create_URL", create_URL))
}
	$("#cma_card_tab_action_container .card-lst").remove();
	$("#cma_card_tab_action_container_action_btn").empty()
	$("#cma_card_tab_action_container_action_btn").append(' <a href="create_URL" class="btn btn-rounded add-row-btn cma_add_button">Add <span class="material-icons">add</span></a>'.replace("create_URL", create_URL))
	   

if ($(this).hasClass('make-active')) {
		$('tbody', $(this)).children('tr').first().addClass('active-row');
		setTimeout(() => {
		  $('.active-row td:visible:last').addClass('last-active-td');
		}, 200);
	  }
	  $(this).closest(".dataTables_wrapper").find(".dt-global-search").on('keyup', function() {
		table.search(this.value).draw();
	  });

		}
			else{
if ($(this).closest(".dataTables_wrapper").find(".dataTables__top .dataTables_buttons").length == 0) {
			$(this).closest(".dataTables_wrapper").find(".dataTables__top").prepend(` 
		
		
		<div class="dataTables_buttons actions">
		 <div class="input-group prefix">
							<span class="input-group-addon">
							<i class="material-icons">search</i>
							</span>
							<input type="text" class="form-control dt-global-search" placeholder="Search..." />
						</div>
		  <div class="dropdown actions__item cma_top_icon">
			<i data-toggle="dropdown" class="material-icons">share</i>
			<ul class="dropdown-menu dropdown-menu-right">
			  <a href="javascript: void 0" class="dropdown-item" data-table-action="excel">Excel (.xlsx)</a>
			  <a href="javascript: void 0" class="dropdown-item" data-table-action="csv">CSV (.csv)</a>
			 
			</ul>
		  </div>
		 <div class="dropdown actions__item cma_top_icon">
									<i data-toggle="dropdown" class="material-icons">settings</i>
									<ul class="dropdown-menu dropdown-menu-right ColVis_collection">
									<li>
																<label> @Resource.arrival_ipdnumber
 <input type="checkbox" class="colvis_switch" data-column="0" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_patient
 <input type="checkbox" class="colvis_switch" data-column="1" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_room
 <input type="checkbox" class="colvis_switch" data-column="2" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_estimatedarrival
 <input type="checkbox" class="colvis_switch" data-column="3" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_bookingstatus
 <input type="checkbox" class="colvis_switch" data-column="4" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_travelarrangement
 <input type="checkbox" class="colvis_switch" data-column="5" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_pickupfrom
 <input type="checkbox" class="colvis_switch" data-column="6" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_wheelchairassistance
 <input type="checkbox" class="colvis_switch" data-column="7" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_requireddinner
 <input type="checkbox" class="colvis_switch" data-column="8" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_specialrequest
 <input type="checkbox" class="colvis_switch" data-column="9" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_paymentstatus
 <input type="checkbox" class="colvis_switch" data-column="10" checked /></label>
															  </li>
<li>
																<label> @Resource.arrival_pendingamount
 <input type="checkbox" class="colvis_switch" data-column="11" checked /></label>
															  </li>
   
									</ul>
									</div>
		</div>
	  `)
}
if ($(this).hasClass('make-active')) {
		$('tbody', $(this)).children('tr').first().addClass('active-row');
		setTimeout(() => {
		  $('.active-row td:visible:last').addClass('last-active-td');
		}, 200);
	  }
	  $(this).closest(".dataTables_wrapper").find(".dt-global-search").on('keyup', function() {
		table.search(this.value).draw();
	  });

		}
if (!$(pagerContainerId).length) {
		$(dataTableId + '_info').parent().append(
					pagerControlsTemplate.replace('^Everything^', totalRecordsCount).replace('^MAXPAGE^', pager_getPageCount())
				);
}
				//$(dataTableId + '_info').hide();
				$(pagerContainerId + ' #pager_lbl_pagecount').text(pager_getPageCount());
				pager_setCurrentPageNumber(1);
				pager_onPageChanged();

			

	}
	 ,columns: [
		 
		{data: 'ipdnumber_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["ipdnumber_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['ipdnumber']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["ipdnumber_master"]+ '</a>' 
}else {
 
return row['ipdnumber_master'] 
}
}
}},
{data: 'patient_master',
mRender: function (data, type, row) {
var Actions_=getAutho('PatientProfile','detail')
 if(row["patient_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "PatientProfile")?PatientProfileid=" + row['patient']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["patient_master"]+ '</a>' 
}else {
 
return row['patient_master'] 
}
}
}},
{data: 'room_master',
mRender: function (data, type, row) {
var Actions_=getAutho('Room','detail')
 if(row["room_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "Room")?Roomid=" + row['room']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["room_master"]+ '</a>' 
}else {
 
return row['room_master'] 
}
}
}},
{data: 'estimatedarrival_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["estimatedarrival_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['estimatedarrival']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["estimatedarrival_master"]+ '</a>' 
}else {
 
return row['estimatedarrival_master'] 
}
}
}},
{data: 'bookingstatus_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["bookingstatus_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['bookingstatus']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["bookingstatus_master"]+ '</a>' 
}else {
 
return row['bookingstatus_master'] 
}
}
}},
{data: 'travelarrangement_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["travelarrangement_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['travelarrangement']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["travelarrangement_master"]+ '</a>' 
}else {
 
return row['travelarrangement_master'] 
}
}
}},
{data: 'pickupfrom_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["pickupfrom_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['pickupfrom']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["pickupfrom_master"]+ '</a>' 
}else {
 
return row['pickupfrom_master'] 
}
}
}},
{data: 'wheelchairassistance_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["wheelchairassistance_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['wheelchairassistance']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["wheelchairassistance_master"]+ '</a>' 
}else {
 
return row['wheelchairassistance_master'] 
}
}
}},
{data: 'requireddinner_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["requireddinner_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['requireddinner']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["requireddinner_master"]+ '</a>' 
}else {
 
return row['requireddinner_master'] 
}
}
}},
{data: 'specialrequest_master',
mRender: function (data, type, row) {
var Actions_=getAutho('IPDApplicationForm','detail')
 if(row["specialrequest_master"]==null) {
 return '-' 
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "IPDApplicationForm")?IPDApplicationFormid=" + row['specialrequest']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["specialrequest_master"]+ '</a>' 
}else {
 
return row['specialrequest_master'] 
}
}
}},
{data: 'paymentstatus_master',
mRender: function (data, type, row) {
var Actions_=getAutho('BillingPayment','detail')
 if(row["paymentstatus_master"]==null) {
 return '-' 
}else if(!row["paymentstatus"]) {
 return row["paymentstatus_master"]
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "BillingPayment")?BillingPaymentid=" + row['paymentstatus']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["paymentstatus_master"]+ '</a>' 
}else {
 
return row['paymentstatus_master'] 
}
}
}},
{data: 'pendingamount_master',
mRender: function (data, type, row) {
var Actions_=getAutho('BillingPayment','detail')
 if(row["pendingamount_master"]==null) {
 return '-' 
}else if(!row["pendingamount"]) {
 return row["pendingamount_master"]
}else {
 
if(Actions_ !=  undefined)
{
var DETAIL_URL = "@Url.Action("Actions_.actionmethodname", "BillingPayment")?BillingPaymentid=" + row['pendingamount']
DETAIL_URL = DETAIL_URL.replace("Actions_.actionmethodname",Actions_.actionmethodname);
return '<a  href="' + DETAIL_URL  + '">'+ row["pendingamount_master"]+ '</a>' 
}else {
 
return row['pendingamount_master'] 
}
}
}},
{data: '_tenantname'},

		{
					"sName": "arrivalid",
					"bSearchable": false,
					"bSortable": false,
					data: 'arrivalid', "className": "dt-center",
					mRender: function(data, type, row) {
					if (!row["arrivalid"] || row["isdirectadmission"] === true) {
						return '-';
					}
					var Actions=getAuthoController("Arrival")
					var appendMessage='';
					if(Actions !=undefined)
					{
					for (var index = 0; index < Actions.length; ++index) {
					var entry = Actions[index];
			 
					if(entry.viewname !="bulkdelete" &&  entry.viewname !="compare" && entry.viewname !="bulkaction" && entry.viewname !="upload" && entry.viewname !="versionhistory" && entry.viewname !="list" && entry.viewname !="list_verify_approved" && entry.viewname !="list_verify" && entry.viewname !="create" && entry.viewname !="post" && entry.viewname !="getall" && entry.viewname !="get" && entry.viewname !="check_user" && entry.viewname !="change_password"){
					var URL = "@Url.Action("entry.viewname", "Arrival")?Arrivalid=" + row["arrivalid"]
					URL = URL.replace("entry.viewname", entry.actionmethodname);
			 
					if(entry.viewname=="delete")
					{
						
						appendMessage+='<a href="' + URL + '" class="dropdown-item deleteRecordFromTable">'+entry.actiondisplayname+'</a>' 	
				   
				
					}
					
					else if(entry.viewname=="detail")
					{
						var DETAIL_URL = "@Url.Action("entry.actionmethodname", "Arrival")?Arrivalid=" + row["arrivalid"]
						DETAIL_URL = DETAIL_URL.replace("entry.actionmethodname", entry.actionmethodname);
						appendMessage+='<a href="' + DETAIL_URL + '" class="dropdown-item">View</a>' 			
 }
					else
					{
					 
						appendMessage+='<a href="' + URL + '" class="dropdown-item">'+entry.actiondisplayname+'</a>' 
					}
			 
					}					 
						 
					}
								
					if(appendMessage =="")
					{
					return '-'

					}
					else
					{
					return '<div class="dropdown actions__item">' +
					'<i data-toggle="dropdown" class="fa fa-ellipsis-h"></i>' +
					'<div class="dropdown-menu dropdown-menu-right">' + appendMessage +
		 

					'</div>' +
					'</div>'
					}



					}
					else
					{
					return '-'

					}


					}
					}
	

	 ],

		ajax: function (data, callback, settings) {
		fetchingDataFromApi = true;
		sendFilterData.searchterm = data.search.value; 
		sendFilterData.pagesize = pageSize;    
		sendFilterData.pagenumber =  (currentPageNumber - 1);   
		var sortParams = [];
		for (var i = 0; i < data.order.length; i++) {
			var colIndex = data.order[i].column;
			var sortDirection = data.order[i].dir;
			var columnName = data.columns[colIndex].data;

			var sortParam = {};
			sortParam["col"] = columnName;
			sortParam["dir"] = sortDirection;

			sortParams.push(sortParam);
		}
		sendFilterData.sortFieldsJson =  JSON.stringify(sortParams);
		$.ajax({
		url: "@Url.Action("get_Arrival_List", "Arrival")",
		type: 'GET',
		dataType: 'json',
		data: sendFilterData,

		 success: function(responsedata) {

				 fetchingDataFromApi = false;
				updateArrivalKpis(responsedata && responsedata.summary);
				//to let grid say no data available and all other flows get through
				if (responsedata.detail == null) {
					responsedata.detail = [];
				}
				totalRecordsCount = responsedata.count;
				if (responsedata.detail.length < pageSize) {
					reachedEOF = true;
				}
		
				setTimeout(function () {
				callback({
					data: responsedata.detail,
					recordsTotal: responsedata.count
				  });
				}, 50);
		},
		error: function(jqXHR, textStatus, errorThrown) {
			fetchingDataFromApi = false;

			if (JSON.stringify(jqXHR.responseText) == "API Authorization failed") {
					swal({
					title: 'Failed!',
					text: jqXHR.responseText,
					type: 'error',
					buttonsStyling: false,
					confirmButtonClass: 'btn btn-primary',
					 allowOutsideClick: false,
						allowEscapeKey: false,
						allowEnterKey: false
					}).then(function() {
					window.location = "@Url.Action("AuthorizationFailed", "users")"
					})
			}
			else
			{
					swal({
					title: 'Contact Administrator !',
					text: jqXHR.responseText,
					type: 'error',
					buttonsStyling: false,
					confirmButtonClass: 'btn btn-primary',
						allowOutsideClick: false,
						allowEscapeKey: false,
						allowEnterKey: false
					})
			}
		}
		});
		},
		"infoCallback": function(settings, start, end, max, total, pre)
		{
				var startRowCount = (pageSize * currentPageNumber) - pageSize + 1;
				var endRowCount = $(dataTableId).DataTable().rows().count() + startRowCount - 1;
				var pagingInfo = 'Showing ' + startRowCount + ' to ' + endRowCount + ' of ' + totalRecordsCount + ' entries';

				return pagingInfo;
		}


})
	 
	  
 $('body').on('click', '.ColVis_collection li', function(a) {
	event.stopPropagation();
  });
 
 $('body').on('change', '.colvis_switch', function(a) {
	var column = table.column($(this).attr('data-column'));
	column.visible(!column.visible());
  });


 

  
	 


	


							
					}
	  function fill_pagination(callback) {
		if ((reachedEOF || fetchingDataFromApi) && currentPageNumber !=1) {
			return;
		}
		fetchingDataFromApi = true;
		 var order = $(dataTableId).DataTable().order();
			var sortParams = [];

			for (var i = 0; i < order.length; i++) {
				var colIndex = order[i][0];
				var sortDirection = order[i][1];
				var columnName = $(dataTableId).DataTable().settings()[0].aoColumns[colIndex].data;

				var sortParam = {
					col: columnName,
					dir: sortDirection
				};

				sortParams.push(sortParam);
			}
			var sendFilterData = {
								"tenantid":($("#tenantid").val() ? $("#tenantid").val() : str_tenantid),
								"ipdnumber":$("#ipdnumber").val(),
"patient":$("#patient").val(),
"room":$("#room").val(),
"estimatedarrival":$("#estimatedarrival").val(),

								"pagesize": pageSize,
								"pagenumber": (currentPageNumber - 1)
								}
			sendFilterData.searchterm = $(".dt-global-search").val();
		sendFilterData.sortFieldsJson= JSON.stringify(sortParams);
		var currentPage = $(dataTableId).DataTable().page();
  
		$.ajax({
			url: "@Url.Action("get_Arrival_List", "Arrival")",
			type: 'GET',
			dataType: 'json',
			data: sendFilterData,
			 success: function (responsedata) {
				updateArrivalKpis(responsedata && responsedata.summary);
				if (responsedata != null && responsedata.detail.length > 0) {
					if (responsedata.detail.length < pageSize) {
						reachedEOF = true;
					}

					api_response_data = responsedata.detail;
					var table = $(dataTableId).DataTable();
					table.rows().remove();
					table.rows.add(responsedata.detail).draw();
				}
				else {
					reachedEOF = true;
				}
				fetchingDataFromApi = false;
				if (callback != undefined) {
					callback();
				}
			},
			error: function (jqXHR, textStatus, errorThrown) {
				fetchingDataFromApi = false;
				if (JSON.stringify(jqXHR.responseText) == "\"API Authorization failed\"") {
					swal({
						title: 'Failed!',
						text: jqXHR.responseText,
						type: 'error',
						buttonsStyling: false,
						confirmButtonClass: 'btn btn-primary',
												allowOutsideClick: false,
						allowEscapeKey: false,
						allowEnterKey: false
					}).then(function () {
						window.location = "@Url.Action("AuthorizationFailed", "users")"
					})
				}
				else {
					swal({
						title: 'Contact Administrator !',
						text: jqXHR.responseText,
						type: 'error',
						buttonsStyling: false,
						confirmButtonClass: 'btn btn-primary',
						 allowOutsideClick: false,
						allowEscapeKey: false,
						allowEnterKey: false
					})
				}
			}
		});


	}



						

					$(document).ready(function () {
					if (!$('#estimatedarrival').val()) {
						$('#estimatedarrival').val(moment().format('DD/MM/YYYY'));
					}
					$('body').on('change', '.dt-global-search', function(event) {
					  reachedEOF = false;

					  pager_setCurrentPageNumber(1);
					  fill_pagination(pager_onPageChanged);

					  $(pagerContainerId + ' #pager_lbl_pagecount').text(pager_getPageCount());
					});


					fill_Arrival()


					var Actions=getAutho("Arrival","list")
					if(Actions == undefined){
					window.location = "@Url.Action("RoleAuthorizationFailed", "users")"
					}




					});
					

					$("body").on("click", "[data-table-action]", function(a) {
					a.preventDefault();
					var b = $(this).data("table-action");
					console.log('dataTables_wrapper', $(this).closest(".dataTables_wrapper").find(".buttons-print"));
					if ("excel" === b && $(this).closest(".dataTables_wrapper").find(".buttons-excel").trigger("click"), "csv" === b && $(this).closest(".dataTables_wrapper").find(".buttons-csv").trigger("click"), "print" === b && $(this).closest(".dataTables_wrapper").find(".buttons-print").trigger("click"), "fullscreen" === b) {
					var c = $(this).closest(".card");
					c.hasClass("card--fullscreen") ? (c.removeClass("card--fullscreen"), $("body").removeClass("data-table-toggled")) : (c.addClass("card--fullscreen"), $("body").addClass("data-table-toggled"))
					}
					});
					</script>
					
					
					
					
								<script>
								$(document).ready(function () {
								$("#tenantid").change(function(){
								fill_Arrival();
								})

								$("#btnFilter").click(function(){
								fill_Arrival();
								})
								});
								</script> 

<script>


// Function to create the option elements
function createOptionElement(value, label, className) {
	var option = document.createElement("option");
	option.value = value;
	option.textContent = label;
	option.className = className;
	return option;
}

// Function to populate the dropdown
function populateDropdown(data) {
	var dropdown = document.getElementById("tenantid");
	dropdown.innerHTML = '<option value=""> Choose </option>';

	// Create a dictionary to store child options grouped by parentid
	var groupedOptions = { };

				// Filter out duplicate entries with the same businessname
				var filteredData = [];
				var uniqueNames = new Set();

				data.forEach(function(item) {
					if (!uniqueNames.has(item.businessname))
					{
						filteredData.push(item);
						uniqueNames.add(item.businessname);
					}
				});

				// Group child options under corresponding parent options
				filteredData.forEach(function(item) {
					if (!item.parentid)
					{
						dropdown.appendChild(createOptionElement(item.tenantid, item.businessname, "optionGroup"));
					}
					else
					{
						if (!groupedOptions[item.parentid])
						{
							groupedOptions[item.parentid] = [];
						}
						groupedOptions[item.parentid].push(item);
					}
				});

				// Add child options to their corresponding parent options
				Object.keys(groupedOptions).forEach(function(parentid) {
					var parentOption = dropdown.querySelector('[value="' + parentid + '"]');
					groupedOptions[parentid].forEach(function(childOption) {
						parentOption.insertAdjacentElement('afterend', createOptionElement(childOption.tenantid, childOption.businessname, "optionChild"));
					});
				});
				
				filteredData.forEach(function (item) {
					if (!item.parentid && !groupedOptions[item.tenantid]) {
						var parentOption = dropdown.querySelector('[value="' + item.tenantid + '"]');
						if(item.natureofbusiness?.indexOf("Lead") !== -1)
						parentOption.classList.add("optionNoChildLead");
						else
						parentOption.classList.add("optionNoChild");
					}
				});

				$("#tenantid").val(str_tenantid);
				$("#tenantid").select2()
				var space = '&nbsp;&nbsp;&nbsp;'
				$("#tenantid").find('.optionChild').prepend(space);
				

			}
$(document).ready(function () {
 if ("@Context.Session.GetString("NalamVazhashowtenant")" =="Y")
 {
$.ajax({
url: '@Url.Action("get_project_tenant","tenant")',
dataType: 'json',
data:{"businesstype": "Healthcare Provider","tenantid": "@Context.Session.GetString("NalamVazhachoosedtenantid")"},
success: function(data) {

    populateDropdown(data);
		
	
 

},
error: function(jqXHR, textStatus, errorThrown) {
if (JSON.stringify(jqXHR.responseText) == "\"API Authorization failed\"")
{
swal({
title: 'Failed!',
text: jqXHR.responseText,
type: 'error',
buttonsStyling: false,
confirmButtonClass: 'btn btn-primary',
allowOutsideClick: false,
allowEscapeKey: false,
allowEnterKey: false
}).then(function() {
window.location = "@Url.Action("AuthorizationFailed", "users")"
}) 
}
else
{
swal({
title: 'Contact Administrator !',
text: jqXHR.responseText,
type: 'error',
buttonsStyling: false,
confirmButtonClass: 'btn btn-primary',
allowOutsideClick: false,
allowEscapeKey: false,
allowEnterKey: false
}) 
}
}
})
}else
{

	$(".tenant_display").hide()
}
});
</script>


					
					
					 
					
					<script>
					var stripdnumber;
					</script>
						@if (Model != null)
						{
							if(Model.ipdnumber != null && Model.ipdnumber.ToString() != "")
							{
							<script>
								 stripdnumber="@Model.ipdnumber"
							</script>
							}
						}
					<script>
					

					function lookup_Arrival_ipdnumber(tenantid) {

					var ddltarget_ipdnumber = $('#ipdnumber');
					ddltarget_ipdnumber.empty();;
					ddltarget_ipdnumber.select2({
						ajax: {
							url: '@Url.Action("lookup_Arrival_ipdnumber")',
							dataType: 'json',
							data: function (params) {

								return {
									"tenantid":tenantid,

									'searchterm': params.term || ''
									,'pagesize': typeaheadpagesize
									,'pagenumber': params.page || 1

								};
							},
							delay: 500,
							processResults: function (data, params) {
								
								if (data.length > 0) {
									data = data.filter(item => item.bookingreferencenumber !== null);
									data.sort((a, b) =>
										String(a.bookingreferencenumber).toLowerCase().localeCompare(String(b.bookingreferencenumber).toLowerCase()) 
									);
								}
								for (i = 0; i < data.length; i++) {
									data[i].id =  data[i].IPDApplicationFormid;
									data[i].name = data[i].bookingreferencenumber;
								}
								return {
									results: data,
									pagination: {
										more: data != null && data.length == typeaheadpagesize
									}
								};
							}
						},
						placeholder: 'Choose',
						minimumResultsForSearch: typeaheadpagesize,
						templateResult: formatSelectOption_lookup_Arrival_ipdnumber,
						 templateSelection: formatSelectSelected_lookup_Arrival_ipdnumber

					});

							  var sendFilterData = {
									"tenantid":tenantid,

									'searchterm': stripdnumber || ''
									,'pagesize': typeaheadpagesize
									,'pagenumber': 1

								};
								$.ajax({
								url: '@Url.Action("lookup_Arrival_ipdnumber")',
								dataType: 'json',
								data: sendFilterData,
								success: function (data) {
									 for (i = 0; i < data.length; i++) {
										if (data[i].IPDApplicationFormid == stripdnumber) {
											var option = new Option(data[i].bookingreferencenumber,data[i].IPDApplicationFormid, true, true);
											ddltarget_ipdnumber.append(option).trigger('update');

											ddltarget_ipdnumber.trigger({
												type: 'select2:select',
												params: {
													data: data
												}
											});
										}
									}
								}
							});


				}

				function formatSelectSelected_lookup_Arrival_ipdnumber(item) {
					 
					if (!item) {
						return 'Choose';
					}
					if (item.text != "") {
						var textValue = item.text;
					}
					else if (item.bookingreferencenumber) {
						var textValue = item.bookingreferencenumber
					} 
					
					return textValue;
				}
				function formatSelectOption_lookup_Arrival_ipdnumber(item) {
					if (item.loading) {
						return item.text;
					}

					var textValue = item.bookingreferencenumber;
					return textValue;

				 }


 
</script>
<script>
						$(document).ready(function(){
						setTimeout(function () {
									lookup_Arrival_ipdnumber(str_tenantid)
								}, 3000)
							
							


						})
						
						</script>

					<script>
					var strpatient;
					</script>
						@if (Model != null)
						{
							if(Model.patient != null && Model.patient.ToString() != "")
							{
							<script>
								 strpatient="@Model.patient"
							</script>
							}
						}
					<script>
					

					function lookup_Arrival_patient(tenantid) {

					var ddltarget_patient = $('#patient');
					ddltarget_patient.empty();;
					ddltarget_patient.select2({
						ajax: {
							url: '@Url.Action("lookup_Arrival_patient")',
							dataType: 'json',
							data: function (params) {

								return {
									"tenantid":tenantid,

									'searchterm': params.term || ''
									,'pagesize': typeaheadpagesize
									,'pagenumber': params.page || 1

								};
							},
							delay: 500,
							processResults: function (data, params) {
								
								if (data.length > 0) {
									data = data.filter(item => item.firstname !== null);
									data.sort((a, b) =>
										String(a.firstname).toLowerCase().localeCompare(String(b.firstname).toLowerCase()) 
									);
								}
								for (i = 0; i < data.length; i++) {
									data[i].id =  data[i].PatientProfileid;
									data[i].name = data[i].firstname+ ' ' + data[i].lastname+ ' ' + data[i].mobilenumber;
								}
								return {
									results: data,
									pagination: {
										more: data != null && data.length == typeaheadpagesize
									}
								};
							}
						},
						placeholder: 'Choose',
						minimumResultsForSearch: typeaheadpagesize,
						templateResult: formatSelectOption_lookup_Arrival_patient,
						 templateSelection: formatSelectSelected_lookup_Arrival_patient

					});

							  var sendFilterData = {
									"tenantid":tenantid,

									'searchterm': strpatient || ''
									,'pagesize': typeaheadpagesize
									,'pagenumber': 1

								};
								$.ajax({
								url: '@Url.Action("lookup_Arrival_patient")',
								dataType: 'json',
								data: sendFilterData,
								success: function (data) {
									 for (i = 0; i < data.length; i++) {
										if (data[i].PatientProfileid == strpatient) {
											var option = new Option(data[i].firstname+ ' ' + data[i].lastname+ ' ' + data[i].mobilenumber,data[i].PatientProfileid, true, true);
											ddltarget_patient.append(option).trigger('update');

											ddltarget_patient.trigger({
												type: 'select2:select',
												params: {
													data: data
												}
											});
										}
									}
								}
							});


				}

				function formatSelectSelected_lookup_Arrival_patient(item) {
					 
					if (!item) {
						return 'Choose';
					}
					if (item.text != "") {
						var textValue = item.text;
					}
					else if (item.firstname+ ' ' + item.lastname+ ' ' + item.mobilenumber) {
						var textValue = item.firstname+ ' ' + item.lastname+ ' ' + item.mobilenumber
					} 
					
					return textValue;
				}
				function formatSelectOption_lookup_Arrival_patient(item) {
					if (item.loading) {
						return item.text;
					}

					var textValue = item.firstname+ ' ' + item.lastname+ ' ' + item.mobilenumber;
					return textValue;

				 }


 
</script>
<script>
						$(document).ready(function(){
						setTimeout(function () {
									lookup_Arrival_patient(str_tenantid)
								}, 3000)
							
							


						})
						
						</script>


<script>
var strroom;
</script>
	@if (Model != null)
	{
		if(Model.room != null && Model.room.ToString() != "")
		{
		<script>
			 strroom="@Model.room"
		</script>
		}
	}
<script>
function lookup_Arrival_room(tenantid) {

var sendFilterData = {
	"tenantid":tenantid
}

$.ajax({
url: '@Url.Action("lookup_Arrival_room")',
dataType: 'json',
data: sendFilterData,
success: function(data) {

		if (data.length > 0) {
			data = data.filter(item => item.roomnumber !== null);
			data.sort((a, b) =>
				String(a.roomnumber).toLowerCase().localeCompare(String(b.roomnumber).toLowerCase()) 
			);
		}
	   var ddltarget_room="#room"
		$(ddltarget_room).empty();
		
		$(ddltarget_room).append("<option value=''>Choose</option>");


		

		$.each(data, function(index, optionDataInner) {

					if(strroom== optionDataInner.Roomid){
					$(ddltarget_room).append('<option selected value="' + optionDataInner.Roomid + '">' + optionDataInner.roomnumber+ '</option>')
					 
                   }
					else
					$(ddltarget_room).append('<option  value="' + optionDataInner.Roomid+ '">' + optionDataInner.roomnumber+ '</option>')


		});

		

	
},
error: function(jqXHR, textStatus, errorThrown) {
if (JSON.stringify(jqXHR.responseText) == "\"API Authorization failed\"")
{
swal({
title: 'Failed!',
text: jqXHR.responseText,
type: 'error',
buttonsStyling: false,
confirmButtonClass: 'btn btn-primary',
allowOutsideClick: false,
allowEscapeKey: false,
allowEnterKey: false
}).then(function() {
window.location = "@Url.Action("AuthorizationFailed", "users")"
}) 
}
else
{
swal({
title: 'Contact Administrator !',
text: jqXHR.responseText,
type: 'error',
buttonsStyling: false,
confirmButtonClass: 'btn btn-primary',
allowOutsideClick: false,
allowEscapeKey: false,
allowEnterKey: false
}) 
}
}
})

}
</script>
<script>
						$(document).ready(function(){

							lookup_Arrival_room(str_tenantid)
							


						})
						
						</script>

					<link href="~/components/xdsoft_datetimepicker/jquery.datetimepicker.css" rel="stylesheet" />
					<script src="~/components/xdsoft_datetimepicker/php-date-formatter.min.js"></script>
					<script src="~/components/xdsoft_datetimepicker/jquery.datetimepicker.js"></script>
					<script>
						$(document).ready(function () {
							$('#estimatedarrival').datetimepicker({
								format: 'd/m/Y',
								formatDate: 'd/m/Y',
								timepicker: false,
								scrollInput: false
							});
						});
					</script>

<script>
					$(document).ready(function () {
						 
						$("#tenantid").change(function(){
								 lookup_Arrival_ipdnumber($("#tenantid").val())
lookup_Arrival_patient($("#tenantid").val())
lookup_Arrival_room($("#tenantid").val())

						})
					

					});
					</script>
